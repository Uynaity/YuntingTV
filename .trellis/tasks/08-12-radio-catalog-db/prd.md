# 电台目录入库

> 唯一事实来源:[radio-proxy/docs/radio-catalog-db.md](../../../radio-proxy/docs/radio-catalog-db.md)
> 本 PRD 不复制该文档的实测数据,只承载需求边界、拆分和跨子任务的验收。
> 与该文档冲突时,**以该文档为准**。

## Goal

把云听 / 蜻蜓 / TuneIn 三个来源的电台目录存进数据库,并按来源纵切,逐来源把读取路径切到库。

## Requirements

原始需求两条:

1. **入库五项:台名、图标、分类、地区、来源。** 其余保持现状。
   ⚠️ 这是清单类需求,**一个字都不要添**。上一轮擅自加了「播放地址」,直接引出签名 TTL 整条废支线。
2. **云听和蜻蜓的 subtitle 改用节目单推断**,不再依赖列表接口现给。

过程中补充三条:

3. 跨区域 / 跨来源搜索。
4. 数据可控(上游改字段 / 返空 / 改分页默认值不能变成事后才知道的用户可见故障)。
5. TuneIn 也定期自动爬,不再手动维护 JSON。

## Constraints

全局规则,四个子任务都适用,来源见文档第五节:

1. 装载是唯一写者,服务端只读这四张表。
2. 库里不存运行时配置(`image` 存上游原始 URL,`publicBase` 读取时拼)。
3. 整份替换:单事务 `DELETE WHERE source=?` + 全量 INSERT。不用 UPSERT。
4. 空目录拒绝写入。
5. 任一子请求失败即整轮放弃(半张目录比写不进去更危险)。
6. 抓取 / 转换 / 服务三段分开。
7. 排序留在 Go,不用 SQL `ORDER BY`。
8. 表结构与读取路径三来源统一,差异只体现在调度,代码里不留分支。
9. **带时效的值不进库**(云听签名播放地址、蜻蜓可由 id 推得的地址,都不存)。
10. 表结构一次建成文档第三节的样子(含 `province_code` 主键 + 独立分类联结表),
    **不要**先建 `category_id` 列再 `ALTER TABLE DROP`——那一步不可逆。

## 拆分与顺序

按**来源纵切**,每一步做完自己就能站住(上一轮按技术层次横切,干完两大步没有任何东西能用):

| 子任务 | 内容 | 优先级 |
|---|---|---|
| [第1步 蜻蜓](../archive/2026-08/08-12-radio-db-s1-qingting/prd.md) | 建表 → 装载 → subtitle 推算 → 定时+告警 → 读取切库 | ✅ 2026-08-13 |
| [第2步 云听](../08-12-radio-db-s2-yunting/prd.md) | 同一条链路 + 320 次枚举 + 起播时解析播放地址 | P1 |
| [第3步 TuneIn](../08-12-radio-db-s3-tunein/prd.md) | 入库 + 读取切库 + 每周自动爬 | P2 |
| [第4步 跨来源搜索](../08-12-radio-db-s4-search/prd.md) | 新功能 | P3 |

顺序约束(父任务负责守住):

- 第 1 步必须先做完:它验证链路形状,第 2 步是复制。
- **每个来源的「读取切库」和「subtitle 换来源」必须在同一子任务内做完。**
  先切读取后换 subtitle,APP 上「正在播放」那行会空——用户可见的功能退化。
- 第 3、4 步不阻塞前两步。

## Acceptance Criteria

跨子任务的验收(各子任务自己的验收在各自 PRD):

- [ ] 四张表结构与文档第三节一致,**无 `play_url_low` 列**
      (唯一偏离:三张表各加一列 `sort_order`,用于复原上游的策展顺序——
      理由与实测见 [第1步 design.md](../archive/2026-08/08-12-radio-db-s1-qingting/design.md) 决策 1。
      **已签字 2026-08-13**,三个来源一致适用)
- [ ] 三个来源共用同一套表结构与读取路径,代码里没有按来源分支
- [ ] 服务端代码中不存在对 `radio_*` 四张表的 INSERT / UPDATE / DELETE
- [ ] 每个来源都有定时刷新 + 失败告警,上游返空 / 抓取失败时不写库且能被发现
- [x] ~~蜻蜓~~ / [ ] 云听 的 subtitle 来自节目单推算,APP 上「正在播放」不为空
- [ ] TuneIn 定期自动爬,不再手工维护 JSON 文件

## Notes

- 第 1 步(蜻蜓)已上线,代码在 `radio-proxy` 仓库的 `feat/radio-db-s1-qingting` 分支;
  云听与 TuneIn 的读取路径尚未切。文档第二节有提交清单。
- 上一轮全部产物在分支 `backup/radio-db-2026-08-12`,是**可用的参考实现**,
  按需 `git show` / `checkout --` / `cherry-pick` 取用,**不要整条合回来**。
  各文件的取用价值见文档第二节的表。
- 作废旧稿 `crawler-db-interface.md` 只在备份分支上,**别照它施工**。
- 文档第四节的实测事实**不用重测**;与之冲突时改别处,不要在代码里绕过去。
- `radio-proxy/` 是独立 git 仓库(父仓库 `.gitignore:33` 排除)。

## 第 1 步(蜻蜓)完成后的遗留

- **人工验收未做完**:APP 真机上翻地区/分类列表看顺序、点开能播。服务端侧已验(见
  [s1 implement.md](../archive/2026-08/08-12-radio-db-s1-qingting/implement.md) 阶段 4),真机那条只能由人做。
- **凌晨 subtitle 缺口未量**:抽样 40 台里 16 台当天首档不是 00:00 起,首档之前推不出
  subtitle;那个时段 live 显示什么没测(测时是白天)。第 2 步做云听时顺手一起量。
- **三条新规则已进交接文档第五节**(规则 9 `sort_order` / 10 抓取与读取分开 / 11 刷新后清缓存),
  其中 9、10 云听会原样撞上,11 是线上踩出来的。
